name: Minimal CI

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - run: echo "hello"
      - run: node --version
      - run: npm --version
