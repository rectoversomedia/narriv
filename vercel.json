{
  "version": 2,
  "buildCommand": "cd frontend && npm install --legacy-peer-deps --include=dev --ignore-scripts && npm run build",
  "outputDirectory": "frontend/.next",
  "installCommand": "cd frontend && npm install --legacy-peer-deps --include=dev",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/backend/src/index.js" }
  ]
}
