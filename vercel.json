{
  "version": 2,
  "buildCommand": "cd frontend && npm install --include=optional && NEXT_DISABLE_TURBOPACK_INDICATION=1 npm run build",
  "outputDirectory": "frontend/.next",
  "installCommand": "cd frontend && npm install",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/backend/src/index.js" }
  ]
}
