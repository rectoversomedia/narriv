{
  "version": 2,
  "buildCommand": "cd frontend && npm install --include=optional && npm run build",
  "outputDirectory": "frontend/.next",
  "installCommand": "cd frontend && npm install --include=optional",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/backend/src/index.js" }
  ]
}
